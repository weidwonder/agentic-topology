<!--
外部 runner 投递说明（这一整段注释不发给被测 Agent，只给操作这套基准的人看）：
- 把下面 “项目路径” 一行替换成 aiudit_platform 仓库在被测 Agent 工作树里的实际绝对路径。
- 这份 prompt 本身不含任何 case 里的节点名、边名或数字——它就是使用者会真的打出来的那句话，
  外加一句指向被测项目的定位信息。不要在投递前往里面加任何提示。
- 投递方式与工具权限必须与其它配置严格一致（见 ../README.md〈公平运行协议〉）。
-->

画一下这个项目的编排。

项目路径：<替换成 aiudit_platform 仓库的绝对路径>
（如果你的工作树里已经打开了这个项目，直接分析当前目录即可，不用等我确认。）

按你已经安装的 agentic-topology 技能的流程走完，产出一份 `.topology.yaml`，并帮我出图。
完成后把 HTML 的绝对路径、几个方块几条连线、以及核对清单里有几处要我自己核实，告诉我。
