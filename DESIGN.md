# 视觉契约

这份文档收的是 agentic-topology 产出的那张图（`<主题>.topology.html`）**必须遵守的视觉规则**。
它们现在散在 `README.zh-CN.md`、`agentic-topology/references/` 与实现代码里，读一条规则要跳好几个
地方对；这里把它们收成一份能拿来单独评审的契约——改视觉呈现之前，先对照这份文档，改完了看是不是
还站得住。

**这不是重写规则，是把已经写在别处的规则收拢**。每一条后面都标了它在仓库里的出处；标了「待确认」
的那节例外——那些是尚未在仓库里找到明文依据的猜测，不能当契约用。

`tools/visual-check.mjs` 是这份契约的检查工具之一：它在真 Chrome 里量出「有没有溢出」「方块和标注
有没有压叠」这类**客观几何事实**，但量不出这份文档里大多数规则（底色对不对、位置像不像结构、正文
是不是 Markdown 这些要靠读代码或人眼判断）。两者不是一回事：`visual-check` 通过不代表这份契约就没
被违反。

---

## 核心视觉纪律

### 1. 底色只表示类型，可信度另走一条通道

> 底色只表示类型；可信度另走虚线边框加徽章，不跟类型抢同一个视觉通道。
> —— `README.zh-CN.md`「它守着哪些边界」

这是项目里最核心的一条视觉纪律。三类执行者各自一种底色——AI 蓝、程序橙、岔路口紫
（`agentic-topology/references/界面用语表.md` 对照表，`agentic-topology/scripts/lib/layout.mjs`
里 `.is-agent` / `.is-program` / `.is-decision` 对应 `CATEGORY_COLORS`）；查得准不准是另一档，
四级（已落地 / 推测 / 缺失 / 设计中）用虚线边框 + 徽章表达，**不改变底色**。校验器拦住
「只有文档来源却标查实了」这类把猜的说成查实的情况（同上出处）。

后果：看一眼底色只能判断"这是谁在干"，判断"这一块信不信得过"必须去看边框是不是虚线、
角标写的是什么档位——两件事永远分两处读，不会因为"这块不确定"就把底色改暗改灰。

### 2. 位置只承载结构，不承载语义

图上的坐标由程序算，表达的是**结构关系**，不是"这个方块更重要"之类的语义判断：

- **分层**：堆（group）之间按边的方向做拓扑排序摆放前后顺序（`layout.mjs`
  `topologicalGroups()`）——排在前面是因为它是别的堆的上游，不是因为它更值得关注。
- **分堆**：同一堆内的方块聚在一起，堆与堆之间留出「连线走廊」（`layout.mjs` 顶部关于
  `ROW_GAP`/`COL_GAP` 的注释：「间距要留得开……看起来就是'线从卡片里穿出来'」）。
- **发起与终止两条轨**：发起路径固定画在画布上方，终止路径（正常收尾/出岔子/被叫停三类）
  固定画在画布下方，「一前一后夹着画布」（README「它到底画了什么」表）。

而"这个方块该不该被特别注意"这件事，走的是另一套独立通道——底色（类型）与虚线/徽章
（可信度），不是位置。

坐标也不是写死的事实：使用者可以拖动方块与堆、点「把位置存回这个文件」写回 HTML，也能一键
「恢复自动摆放」（README「它到底画了什么」表）。位置能被人随意改而不影响图的事实内容，
正说明它不是语义通道的一部分——它是呈现层，事实住在别处（底色、边框、连线上的信息引用）。

### 3. 不静默裁掉方块或连线

> 不静默裁掉东西：方块多了靠分堆折叠和筛选解决，折叠后再展开逐个 ID 相同。
> —— `README.zh-CN.md`「它守着哪些边界」

图变复杂时，应对手段是折叠（收进堆）和筛选（按类型/可信度/分堆勾选），不是丢弃。折叠只是
收起显示，展开后方块与连线的 ID 与原来逐一对应——这条已经在最近一轮改动里扩展到「筛选也带上
标注」（见提交历史 `05c342b`：`筛选也带上标注`）。

### 4. 线上标注至多三份，多了收成「等 N 份」

> 线上写它的名字加一个字的交法小标（至多三份，多了收成「等 N 份」）。
> —— `README.zh-CN.md`「它到底画了什么」表

实测印证：本仓 `agentic-topology/tests/fixtures/aiudit-internal-control.topology.yaml`
渲染出的图上，超过三份信息的连线标注确实收成了「📦 沿用的 AuditL… 📦 进度文案「已沿用上次…
📦 AuditLedge… 等 4 份」这种形式（渲染产物实测，`tools/visual-check.mjs` 跑这份 fixture 时
量到的原文）。

### 5. 正文字段一律走 Markdown

> 正文字段（方块、详情、发起、收尾、停止条件、每份信息）一律走 Markdown，长句子在图上不会被
> YAML 里的手工折行带偏。
> —— `README.zh-CN.md`「它到底画了什么」表

配套的边界：`--lang en` **不翻译**描述文件里的正文——方块名、职责、信息说明都是被分析项目
自己的话，翻译一道就不再是事实（`agentic-topology/references/界面用语表.md`「四条硬规矩」
第 4 条）。同一份文件里，`tools`/`mcp`/`skills` 数组、文件名、函数名这类标识符也不翻译，
理由是「这些是使用者要拿去在代码里搜的字符串」（同上「四条硬规矩」第 3 条）。

### 6. 零依赖、不联网、断网能看

> 零依赖，产物是单文件 HTML，断网能看；页面里的 Markdown 也是自带的极简渲染器，不走 CDN、
> 不引任何外部资源。
> —— `README.zh-CN.md`「它守着哪些边界」

配合安装时的要求：「Node ≥ 18。零 npm 依赖，不联网，不上传你的代码」（README「安装」节）。
这一条也约束了这份契约本身允许的实现手段——不能为了实现某个视觉效果去引入一个 CDN 字体或
一个外部图标库。

---

## 反面清单

这节写的是这个项目**不想变成什么**。参照 [tt-a1i/archify](https://github.com/tt-a1i/archify)
`PRODUCT.md` 的 anti-references 写法：明说拒绝什么，比只说做什么更防得住"以后顺手就做了"。

- **不做通用绘图 / 流程图工具。** 这个技能的定位是"把一个 agentic 应用的编排画出来"，不是
  一个可以画任意图的画布。`SKILL.md` 的 frontmatter 原话：「它只陈述读到的事实，不评价架构
  好坏、不给改进建议，也不做与 agent 编排无关的通用绘图或流程图。」加任何一个"顺便也能画
  XX 图"的通用能力，都是在偏离这条。

- **不做动效，不做"故事章节"式的引导播放。** 当前实现里没有任何 `transition` / `animation` /
  `@keyframes`（`assets/page-shell/*.css` 与 `assets/page-shell/app.js` 全文核实，均无匹配）。
  这不是漏做，是要刻意守住的边界：动效和分章节播放会暗示画面里有一个源码里其实不存在的
  "时序"或"叙事顺序"——而这张图画的是静态编排结构，不是一次运行的回放。（本条是这份文档
  首次写下，不是转述仓库别处已有的明文规则；理由建立在上面已核实的事实之上：现状零动效 +
  项目「只陈述事实」的一贯立场。）

- **不给架构好坏的判断。**
  > 它只告诉你它查到了什么，不告诉你它觉得怎么样。
  > 它不评价架构好坏，也不给改进建议。用途是让你看清楚到能自己下判断。
  > —— `README.zh-CN.md`
  任何"这条链路设计得不好""这个 Agent 权限太大"之类的评价性文案，不管多委婉，都不属于这张图。

- **不为了让链路看起来顺，把两份信息悄悄并掉。**
  > 读不出两处是不是同一份时，拆开、两份都标「只是猜的」、其中一份指向另一份，并在核对清单
  > 里点名这两份。它不会为了让链路看起来顺就悄悄并掉。
  > —— `README.zh-CN.md`「它到底画了什么」表
  宁可图上多一份「可能是同一份」的存疑标注，也不能让看图的人以为两处天然就是一回事。

---

## 待确认

这节记的是分析过程中冒出来、但没能在仓库里找到明文依据的猜测——**不当契约用，只留个待核实的
钩子**，将来谁要动相关代码，先去核实再决定要不要转正。

- **折叠堆是否也遵守"至多三份标注"的规则。** 「核心视觉纪律」第 4 条的原文只提到"线上"的
  信息标注；折叠视图里堆与堆之间的连线标注实测同样出现了「等 N 份」的收纳形式（如
  `analysis` 渲染出的「📦 沿用的 AuditL… 📦 进度文案「已沿用上次… 📦 AuditLedge… 等 4 份」），
  但没找到一处文档专门声明"折叠视图的标注规则与展开视图完全一致"——目前只是观察到两处实现
  用的是同一段渲染逻辑，没有单独的文字契约可引用。

- **`.topo-wrap` 画布容器允许横向内部滚动，是不是设计如此。** `tools/visual-check.mjs` 实测
  发现：折叠视图的画布在全部四个桌面视口（含 2560×1440）下都需要在 `.topo-wrap` 内部横向
  滚动才能看全（`.topology-page { max-width: 1560px }` 卡住了可用宽度，折叠视图的堆间距
  又把画布撑到了约 1546px 宽 + padding）。没找到文档说明这是刻意的"画布本来就该在窄列里横向
  滚"，还是遗漏。这一条只报现象，不下结论——按任务边界，改 `agentic-topology/` 下的实现不在
  这次改动范围内。
